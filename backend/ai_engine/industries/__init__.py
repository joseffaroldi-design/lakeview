"""Industries package — business-specific prompts and helpers.

Add a new industry by creating `<name>.py` with a `SYSTEM_PROMPT` constant
(and optionally additional template/helper functions). Then register it
in ai_engine.prompts.resolve_system_prompt().
"""
